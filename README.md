# Atari

Reinforcement learning experiments built on [Gymnasium](https://gymnasium.farama.org/).

## Setup

```bash
python -m venv .venv
.venv\Scripts\activate        # Windows
# source .venv/bin/activate   # macOS / Linux

pip install "gymnasium[atari]"
```

Requires Python 3.10 or newer.

## Usage

_TODO_
